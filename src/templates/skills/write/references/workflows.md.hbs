# Write 工作流程

## 概述

Write 阶段基于情节架构进行小说正文创作，支持单章写作、批量写作和偏离检查。

每章写作采用**三明治架构**：输入约束（上层面包）→ 概率生成（中间的肉）→ 输出验证（下层面包），确保正文质量。

---

## 写作前检查

### 流程

```
1. 确认写作目标：
   - 用户指定章节号 / 继续写作 / 批量写作
2. 检查章节细纲：
   a. 有细纲 → 读取并展示摘要
   b. 无细纲 → 检查是否有分卷大纲
      i. 有分卷大纲 → 基于分卷大纲自动生成细纲
      ii. 无分卷大纲 → 基于核心创意生成简单细纲
3. 【感知阶段】读取上下文并构建预测模型：
   a. 按优先级加载文件（P0 > P1 > P2）
   b. 输出"情节预期"（100 字以内）
   c. 展示预测模型给用户确认
4. 【上层面包】输入约束检查：
   - 核心冲突、出场角色、关键情节点、字数目标是否齐全
   - 缺少则暂停询问用户
5. 展示细纲摘要给用户确认
6. 用户确认后开始写作
```

### 感知阶段详细说明

#### 上下文窗口管理

写作时需要控制上下文，避免超出窗口：

```
优先级（从高到低）：
P0（必须）：
  1. 本章细纲
  2. 上一章最后 500 字（确保连贯）
  3. style.md 风格标签

P1（推荐）：
  4. 涉及角色信息（仅本章出场角色）
  5. core_idea.md 核心创意一句话

P2（可选）：
  6. 涉及世界观信息
  7. 前文未回收伏笔

Token 预算：总量不超过 7000 token。
超出时按 P2 → P1 顺序截断。
估算公式：中文字数 × 1.5 + 英文单词数 + 标点符号数 = 估算 token 数
```

#### 预测模型格式

```
【情节预期】
- 核心冲突：[一句话]
- 角色预期行为：[角色名] → [预期行为]
- 伏笔回收：[如有]
- 本章情绪走向：[如"从紧张到释然"]
```

#### 惊奇点检测

生成过程中实时监控，遇到以下情况暂停：

| 惊奇类型 | 判断标准 |
|----------|----------|
| 角色行为偏离 | 角色行为与档案描述矛盾 |
| 情节超出细纲 | 出现细纲中未计划的重要事件 |
| 世界观冲突 | 违反已建立的世界观规则 |
| 风格偏离 | 与 style.md 设定的风格不匹配 |

暂停后提供三个选项：确认偏离 / 修改 / 重新生成

---

## 写作后检查

### 触发时机
- **自动执行**：每章写作完成后自动触发（三明治架构下层面包）
- 用户手动请求（`/write check`）

### 流程

```
1. 【下层面包】自动验证：
   a. 一致性检查（必须通过）：
      - 角色行为是否符合 characters/ 档案
      - 世界观规则是否与 world/ 设定一致
      - 细纲核心事件是否全部覆盖
      - 前后文衔接是否自然
   b. 衔接性检查（必须通过）：
      - 章首是否承接上一章末尾的情绪/场景/悬念
      - 章末是否设置了下一章的钩子
      - 章内情绪转折是否有过渡段，无突兀跳变
      - 叙事视角是否与上一章一致（或有过渡切换）
   c. 文学性检查（必须通过 — 去 AI 味）：
      - 是否存在 AI 高频词（宛如、不禁、淡淡的、微微等）
      - 是否存在解释式情感（"他感到X"、"她心中涌起Y"）
      - 句长是否有变异，无连续 3 句长度几乎相同
      - 出场角色对话风格是否有区分度
      - 是否有至少 3 处具体的感官/动作/物件细节（骨血评分）
   d. 质量检查（建议通过）：
      - 字数是否在目标 ±20% 范围内
      - 节奏控制是否张弛有度
      - 对话是否自然
2. 纠偏循环：
   a. 一致性、衔接性或文学性检查失败 → 自动修正 → 重新验证（最多 2 次）
   b. 2 次修正后仍失败 → 暂停，向用户报告具体问题
   c. 质量不达标 → 提示用户，由用户决定是否修正
3. 输出验证报告
4. 展示给用户确认
```

### 验证报告格式

```
【第 N 章 验证报告】

一致性检查：
✅ 角色行为一致
✅ 世界观规则无冲突
✅ 细纲核心事件已覆盖（3/3）
✅ 前后文衔接自然

文学性检查（去AI味）：
✅ AI 高频词：未发现禁用词
✅ 情感表达：无解释式情感
✅ 句长变异：分布合理
✅ 对话口癖：角色风格有区分
✅ 骨血评分：★★★★☆（4/5）

质量检查：
✅ 字数：3200 字（目标 2500-3500）
✅ 节奏控制合理
✅ 对话自然

总体评价：✅ 全部通过
```

### 偏离处理

```
一致性检查失败（自动纠偏）：
  第 1 次失败 → 自动修正问题段落 → 重新验证
  第 2 次失败 → 再次修正 → 重新验证
  2 次后仍失败 → 暂停，向用户报告：
    "自动修正未能解决以下问题：
     - [问题 1]
     - [问题 2]
     请选择：
     1. 人工修改（我帮你改）
     2. 跳过验证，保存当前版本
     3. 完全重新生成本章"

质量不达标（提示用户）：
  → 提示具体问题
  → 由用户决定是否修正
  → 不强制自动修正（保持创作自由度）

显著偏离（影响后续）：
  → 展示偏离详情 + 影响分析
  → 提供 3 个选项：
    1. 自动更新后续细纲（推荐）
    2. 修改正文以贴合细纲
    3. 标记为有意偏离，暂不处理
```

---

## 进度追踪

### 流程

```
1. 扫描 chapters/drafts/ 目录
2. 按章节号排序，统计已写章节
3. 检查每章字数是否在范围内
   - 范围：{{writing.chapter.word_count_min}} ~ {{writing.chapter.word_count_max}} 字
4. 计算总进度
5. 展示进度信息
```

### 进度输出格式

```
【写作进度】

总进度：X/Y 章 ████████░░░░░░░░░░░░ XX%
当前卷：第 N 卷

【已写章节】
第 1 章：[标题] - XXXX 字 ✅
第 2 章：[标题] - XXXX 字 ✅
第 3 章：[标题] - XXXX 字 ⚠️ (字数偏少)

【待写章节】
第 4 章：有细纲
第 5 章：无细纲
第 6 章：无细纲

【下一步建议】
→ /write 4 - 写第 4 章
→ /write outline 4-6 - 生成第 4-6 章细纲
```

---

## 版本管理

### 自动备份
- 修改前自动备份到 `chapters/drafts/archive/`
- 备份命名：`chapter_NNN-backup-YYYY-MM-DD-HHMMSS.md`
- 保留最近 {{writing.backup_count}} 个备份

### 撤销
```
/write undo → 恢复到上一个备份版本
/write undo N → 恢复第 N 章到上一个版本
```

### 历史查看
```
/write history → 查看所有章节的备份记录
/write history N → 查看第 N 章的备份记录
```
