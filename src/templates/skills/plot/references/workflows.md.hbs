# Plot 工作流程

## 概述

Plot 阶段负责基于创作宪法生成完整的情节架构，包括主大纲、分卷大纲和章节细纲。

---

## 完整大纲生成

### 前置检查

```
1. 检查 plot_intent.md 是否存在 → 不存在则报错
2. 检查 world/{{versioning.initial_version}}.md 是否存在 → 不存在则警告
3. 检查 characters/{{versioning.initial_version}}.md 是否存在 → 不存在则警告
4. 检查 style.md 是否存在 → 不存在则使用默认风格
5. 检查 outlines/{{versioning.initial_version}}/ 是否已有内容
   → 已有则提示"大纲已存在，是否覆盖？"
```
6. 扫描 chapters/drafts/ 目录
   → 如有已写章节，记录最大章节号和最后 500 字
   → 新大纲必须与已写内容衔接，不得推翻已写情节

### 生成流程

```
1. 读取所有创作宪法文件
2. 基于 plot_intent.md 的篇幅分配生成主大纲
   - 使用 Freytag 金字塔五幕结构
   - 确保与世界观/角色设定一致
3. 基于主大纲生成分卷大纲
   - 每章一句话概述
   - 标注冲突密度（低/中/高）
4. 保存文件：
   - outlines/{{versioning.initial_version}}/master_outline.md
   - outlines/{{versioning.initial_version}}/volume_1_outline.md
   - outlines/{{versioning.initial_version}}/volume_2_outline.md
   - ...
5. 【情节自检】自动执行大纲验证：
   a. 一致性检查：世界观合规、角色行为、因果逻辑
   b. 结构检查：冲突链、角色弧线、伏笔闭环、节奏曲线
   c. 质量检查：可写性、悬念设置
   d. 输出自检报告
6. 展示大纲摘要 + 自检报告
7. 用户确认
```

### 用户介入点
- 大纲摘要确认：展示主大纲和各卷概要
- 可要求调整特定部分

---

## 探索模式

### 前置检查
与完整大纲生成相同。

### 生成流程

```
1. 读取所有创作宪法文件
2. 生成 3 个不同方向的大纲方案：
   - 方案 A（保守）：忠于 plot_intent.md，稳妥推进
   - 方案 B（平衡）：在 plot_intent.md 基础上适当创新
   - 方案 C（大胆）：大胆改编，可能偏离原有方向
3. 每个方案包含：
   - 一句话概括
   - 主线走向
   - 关键转折
   - 预期篇幅
   - 难度评级
4. 保存到 outlines/proposals/
5. 展示对比表格
6. 用户选择
7. 将选中的方案升级为完整大纲
```

### 用户介入点
- 方案选择：展示 3 个方案对比，用户选择
- 可查看方案详情后再决定

---

## 章节细纲生成

### 前置检查

```
1. 检查主大纲是否存在 → 不存在则提示先生成主大纲
2. 检查对应的分卷大纲是否存在 → 不存在则提示先生成分卷大纲
3. 检查目标章节细纲是否已存在
   → 已存在则提示"细纲已存在，是否重新生成？"
4. 扫描 chapters/drafts/ 目录
   → 如有已写章节，将最近一章末尾 500 字注入上下文
   → 确保新生成的细纲与已写内容无缝衔接
```

### 生成流程

```
1. 读取分卷大纲
2. 确定要生成的章节范围
   - 默认：接下来 {{writing.chapter_batch}} 章
   - 用户指定：按用户指定范围
3. 逐章生成细纲：
   a. 读取上一章细纲（如有），确保连贯
   b. 基于分卷大纲的本章一句话概述展开
   c. 添加：核心事件、节奏控制、伏笔、场景切换点
   d. 检查与世界观/角色设定的一致性
4. **情节自检**（自动执行，详见 plot.skill.hbs "情节自检"章节）
5. 批量展示细纲给用户确认（附自检报告）
6. 用户确认后保存到 chapters/outlines/chapter_NNN.md
```

### 用户介入点
- 批量细纲确认：展示所有待生成细纲，用户确认
- 可修改个别章节的细纲

---

## 版本管理

### 自动备份
- 修改前自动备份到 `outlines/drafts/`
- 备份命名：`backup-YYYY-MM-DD-HHMMSS`
- 保留最近 {{writing.backup_count}} 个备份

### 历史版本
- 历史版本归档到 `outlines/archives/`
- 通过 `/plot history` 查看

### 撤销
- `/plot undo` 恢复到上一个备份版本

---

## 情节自检（大纲/细纲生成后自动执行）

### 触发时机
- **自动执行**：每次生成或修改大纲/细纲后自动触发
- 不等待用户触发，作为生成流程的最后一步

### 检查流程

```
1. 一致性检查（必须通过）：
   a. 世界观合规：情节事件是否违反 world/ 中的规则设定
   b. 角色行为：关键情节中的角色行为是否符合 characters/ 档案
   c. 因果逻辑：事件之间是否有清晰的因果链，无孤立事件
2. 结构检查（必须通过）：
   a. 冲突链：每幕/每章是否有核心冲突，冲突间是否有递进
   b. 角色弧线：主角是否有起点→变化→终点的完整轨迹
   c. 伏笔闭环：所有伏笔是否有明确的回收计划
   d. 节奏曲线：高潮/低谷交替，无连续多章同强度
3. 质量检查（建议通过）：
   a. 可写性：细纲场景细节是否足够供 write 使用
   b. 悬念设置：是否每章末有钩子
   c. 新鲜度：是否有意外元素避免完全可预测
4. 纠偏：
   a. 一致性或结构检查失败 → 自动修正 → 重新验证（最多 1 次）
   b. 1 次修正后仍失败 → 暂停，向用户报告
   c. 质量不达标 → 提示用户
5. 输出自检报告
```

### 自检报告格式

```
【情节自检报告】

一致性检查：
✅ 世界观合规 — 无违反规则事件
✅ 角色行为 — 主要角色行为符合档案
✅ 因果逻辑 — 事件间因果链完整

结构检查：
✅ 冲突链 — 5 幕均有核心冲突且递进合理
⚠️ 角色弧线 — 反派"XXX"缺少变化轨迹，建议补充
✅ 伏笔闭环 — 6 条伏笔均有回收计划
✅ 节奏曲线 — 高低交替合理

质量检查：
✅ 可写性 — 场景细节充足
✅ 悬念设置 — 每章末有钩子

总体评价：✅ 通过（1 项建议）
建议：为反派"XXX"补充动机变化弧线
```
