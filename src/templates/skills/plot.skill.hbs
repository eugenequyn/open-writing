---
name: plot
description: "情节架构命令 - 设计引人入胜、逻辑自洽的情节结构。使用场景：(1) Builder 完成后设计完整情节 (2) 探索多种情节方案 (3) 生成带情感曲线的章节细纲 (4) 查看/撤销大纲历史版本 (5) 查看/调整进度 (6) 接收写作偏离反馈并修正后续细纲。支持命令别名：/p"
disable-model-invocation: true
---

# Plot Skill - 设计引人入胜的情节

基于已完成的创作宪法（世界观、角色、情节意向），设计有冲突张力、角色弧线和伏笔体系的情节结构。

**核心设计子目标**（每一次大纲生成都必须追求）：

1. **冲突张力**：每幕/每章都有明确的核心冲突，冲突间有因果递进，读者始终有"接下来会怎样"的悬念
2. **角色弧线**：主要角色在故事中有完整的成长/变化轨迹，且变化由情节事件驱动而非作者强加
3. **伏笔体系**：关键伏笔有明确的埋设和回收计划，形成叙事闭环，不留下"遗忘的线头"
4. **节奏控制**：张弛有度，高密度冲突与缓冲交替，避免持续紧绷或长期平淡
5. **世界观一致性**：所有情节事件符合已建立的世界观规则，不为了"酷"而违反设定

> **前置条件**：运行 `/builder` 完成后才能使用 `/plot`
>
> **依赖文件**：
> - `plot_intent.md` - 情节意向书（必需）
> - `world/{{versioning.initial_version}}.md` - 世界观设定（必需）
> - `characters/{{versioning.initial_version}}.md` - 角色档案（必需）

## 快速开始

```bash
/plot                       # 显示帮助菜单（默认）
/plot full                  # 直接生成完整大纲
/p                          # 命令别名，同上
/plot explore               # 生成 3 个方案供选择
/plot chapters              # 生成接下来 {{writing.chapter_batch}} 章细纲
/plot chapters 1-10         # 生成第 1-10 章细纲
/plot history               # 查看历史版本
/plot undo                  # 撤销上次修改
/plot --status              # 查看大纲进度状态
```

---

## 无参数帮助菜单

当用户输入 `/plot` 无参数时，显示帮助菜单：

```
【Plot Skill - 情节架构命令】

你想做什么？

【生成大纲】
  1. /plot full             - 直接生成完整大纲（推荐）
  2. /plot explore          - 生成 3 个方案对比选择

【章节细纲】
  3. /plot chapters         - 生成接下来 {{writing.chapter_batch}} 章细纲
  4. /plot chapters 1-10    - 生成指定范围细纲
  5. /plot chapters 5       - 生成第 5 章细纲

【查看状态】
  6. /plot --status         - 查看大纲进度
  7. /plot history          - 查看历史版本
  8. /plot undo             - 撤销上次修改

【快捷命令】
  /p 是 /plot 的快捷别名

💡 提示：首次使用建议运行 /plot full 生成完整大纲
```

## 感知阶段（情节设计前首要步骤）

**在开始任何情节设计之前，你必须先读取以下文件理解创作上下文：**

| 优先级 | 文件 | 说明 |
|--------|------|------|
| P0（必须） | `plot_intent.md` | 情节意向，核心矛盾和目标 |
| P0（必须） | `core_idea.md` | 核心创意一句话 |
| P1（推荐） | `world/{{versioning.initial_version}}.md` | 世界观设定摘要 |
| P1（推荐） | `characters/{{versioning.initial_version}}.md` | 主要角色档案 |
| P2（可选） | `style.md` | 风格约束和叙事偏好 |
| P2（可选） | 已写章节末尾（如有） | 确保新情节与已写内容衔接 |

**感知输出**：读取后，先输出 150 字以内的"情节设计预期"，包括：

```
【情节设计预期】
- 核心矛盾：[故事要解决的根本冲突]
- 主角弧线预期：[角色起点] → [角色终点]
- 关键转折预判：[至少 2 个重大转折的雏形]
- 情感基调：[整体情感走向，如"从黑暗到希望"]
- 潜在风险点：[哪些情节可能难写或难以自洽]
```

**等待用户确认后再继续生成。**

---

## 核心命令

### `/plot` - 生成完整大纲

```bash
/plot                       # 默认模式
/plot full                  # 完整大纲（同上）
```

**输出**：
- `outlines/{{versioning.initial_version}}/master_outline.md` - 主大纲（Freytag 金字塔结构）
- `outlines/{{versioning.initial_version}}/volume_X_outline.md` - 分卷大纲（每章一句话概述）

**适用场景**：用户希望有完整大纲再开始写作

---

### `/plot explore` - 探索模式

```bash
/plot explore
```

**输出**：
- `outlines/proposals/outline_A.md` - 方案 A（保守方案）
- `outlines/proposals/outline_B.md` - 方案 B（平衡方案）
- `outlines/proposals/outline_C.md` - 方案 C（大胆方案）

**后续操作**：系统展示方案摘要，用户选择后自动升级为完整大纲

**适用场景**：用户不确定故事走向，想看看多种可能

---

### `/plot chapters` - 生成章节细纲

```bash
/plot chapters              # 生成接下来 {{writing.chapter_batch}} 章细纲
/plot chapters --all        # 生成全部章节细纲
/plot chapters 1-10         # 生成第 1-10 章细纲
/plot chapters 5            # 生成第 5 章细纲
```

**输出**：
- `chapters/outlines/chapter_XXX.md` - 章节细纲（包含核心事件、节奏控制、伏笔）

**适用场景**：写作前准备细纲

---

### `/plot history` - 查看历史

```bash
/plot history
```

**输出**：历史版本列表（只读）

---

### `/plot undo` - 撤销

```bash
/plot undo
```

**功能**：撤销上一次大纲修改，恢复到前一版本

---

### `/plot --status` - 查看进度

```bash
/plot --status
```

**输出**：
- 当前大纲版本信息
- 主大纲完成状态
- 分卷大纲列表
- 章节细纲进度
- 下一步建议

---

## 工作流程

### 完整大纲生成

```
/plot → 检查前置条件 → 读取创作宪法 → 生成大纲 → 保存 outlines/{{versioning.initial_version}}/ → 提示下一步
```

**详细流程**：详见 [references/workflows.md](references/workflows.md#完整大纲生成)

---

### 探索模式

```
/plot explore → 检查前置条件 → 生成 3 个方案 → 保存 proposals/ → 展示摘要 → 用户选择 → 升级为完整大纲
```

**详细流程**：详见 [references/workflows.md](references/workflows.md#探索模式)

---

### 章节细纲生成

```
/plot chapters → 检查主大纲/分卷大纲 → 读取 volume 大纲 → 生成章节细纲 → 保存 chapters/outlines/
```

**详细流程**：详见 [references/workflows.md](references/workflows.md#章节细纲生成)

---

## 输出模板

| 模板 | 用途 | 文件 |
|------|------|------|
| 主大纲 | Freytag 金字塔五幕结构 | [references/master-outline-template.md](references/master-outline-template.md) |
| 分卷大纲 | 每章一句话概述 + 冲突密度 | [references/volume-outline-template.md](references/volume-outline-template.md) |
| 章节细纲 | 详细事件/节奏/伏笔 | [references/chapter-outline-template.md](references/chapter-outline-template.md) |

---

## 交互式引导

### 首次运行

```
系统：检测到你的创作宪法已完成！

你想怎么生成大纲？
  1. 直接生成完整大纲（推荐）
  2. 先看 3 种不同方案再决定
  3. 一步一步来，每步确认
```

### 完成后

```
系统：✅ 大纲已生成！

【大纲摘要】
- 主大纲：Freytag 五幕结构完整
- 第一卷：30 章，每章一句话概述

接下来你可以：
  1. /write 1 - 开始写第 1 章（推荐）
  2. /plot chapters 1-3 - 预览前 3 章细纲
  3. /plot explore - 看看其他方案
  4. /plot --status - 查看大纲状态
```

### Explore 模式后

```
系统：✅ 已生成 3 个情节方案！

【方案对比】
| 维度 | 方案 A（保守） | 方案 B（平衡） | 方案 C（大胆） |
|------|--------------|--------------|--------------|
| 主线 | 复仇驱动 | 成长驱动 | 悬疑驱动 |
| 节奏 | 稳步推进 | 张弛有度 | 快节奏 |
| 难度 | ⭐⭐ 易驾驭 | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐ 挑战性 |
| 预期篇幅 | 30 章 | 60 章 | 100 章 + |
| 推荐度 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

你想：
  1. 选择方案 A 并生成完整大纲
  2. 选择方案 B 并生成完整大纲（推荐）
  3. 选择方案 C 并生成完整大纲
  4. 查看方案详细对比
  5. 先不决定，稍后再说
```

### Undo 操作后

```
系统：✅ 已撤销到 backup-2026-03-10-092115

【撤销内容】
- 修改了第一卷大纲第 5-10 章的情节走向

接下来你可以：
  1. /plot undo - 继续撤销到上一个版本
  2. /write 1 - 基于当前大纲开始写作
  3. /plot chapters 1-5 - 重新生成前 5 章细纲
```

### 依赖变化检测后

```
系统：检测到 world/{{versioning.initial_version}}.md 刚刚更新：
- 修改内容：能力体系增加"记忆清除"限制

这会影响当前大纲中第 15 章和第 20 章的情节设计。

你想：
  1. 自动检查并修复冲突（推荐）
  2. 我自己检查，稍后处理
  3. 忽略，继续写作
```

---

## 情节自检（大纲生成后自动执行）

**每次生成或修改大纲后，自动执行以下验证，不等待用户触发：**

### 一致性检查（必须通过）

| 检查项 | 标准 | 失败处理 |
|--------|------|----------|
| 世界观合规 | 情节事件是否违反 `world/` 规则 | 标记违规点，提示修正 |
| 角色行为 | 关键情节中的角色行为是否符合 `characters/` 档案 | 标记偏离，提示调整 |
| 因果逻辑 | 事件之间是否有清晰的因果链，无"突然发生"的孤立事件 | 要求补充因果关系 |

### 结构检查（必须通过）

| 检查项 | 标准 | 失败处理 |
|--------|------|----------|
| 冲突链完整 | 每幕都有核心冲突，冲突间有递进关系 | 标记缺失冲突的幕/章 |
| 角色弧线 | 主角是否有起点→变化→终点的完整弧线 | 标记弧线断裂处 |
| 伏笔闭环 | 所有埋设的伏笔是否有明确的回收计划 | 列出未闭环的伏笔 |
| 节奏曲线 | 高潮/低谷交替，无连续多章同强度 | 标记节奏单调区间 |

### 质量检查（建议通过）

| 检查项 | 标准 | 失败处理 |
|--------|------|----------|
| 可写性 | 细纲是否有足够的场景细节供 write 使用 | 提示补充 |
| 悬念设置 | 是否有足够"钩子"驱动读者继续 | 提示 |
| 新鲜度 | 情节是否有意外元素，避免完全可预测 | 提示 |

### 自检报告格式

```
【大纲/细纲自检报告】

一致性检查：
✅ 世界观合规 — 无违反规则事件
✅ 角色行为一致 — 主要角色行为符合档案
✅ 因果逻辑 — 事件间因果链完整

结构检查：
✅ 冲突链完整 — 5 幕均有核心冲突
⚠️ 角色弧线 — 反派"沈墨"缺少变化轨迹，建议补充
✅ 伏笔闭环 — 6 条伏笔均有回收计划
✅ 节奏曲线 — 高低交替合理

质量检查：
✅ 可写性 — 场景细节充足
✅ 悬念设置 — 每章末有钩子

总体评价：✅ 通过（1 项建议）
建议：为反派"沈墨"补充动机变化，使结局更有说服力
```

---

## 版本管理（自动）

系统自动处理：
- 修改前自动备份到 `outlines/drafts/`
- 保留最近 3 个备份
- 历史版本归档到 `outlines/archives/`

用户无需手动管理版本。

---

## 依赖检查

当世界观/角色修改时：

```
系统：检测到 world/{{versioning.initial_version}}.md 刚刚更新：
- 修改内容：能力体系增加"记忆清除"限制

这会影响当前大纲的情节设计。

你想：
  1. 自动检查并修复冲突（推荐）
  2. 我自己检查，稍后处理
  3. 忽略，继续写作
```

---

## Builder vs Plot vs Write

| 场景 | 使用工具 |
|------|---------|
| 首次创建项目（世界观 + 角色） | Builder |
| 情节架构（大纲 + 细纲） | Plot |
| 写作 | Write |
| 日常修改（直接编辑文件） | 直接编辑 world/、characters/、outlines/ 目录下的文件 |

---

## 详细文档

- **工作流程**：[references/workflows.md](references/workflows.md)
- **主大纲模板**：[references/master-outline-template.md](references/master-outline-template.md)
- **分卷大纲模板**：[references/volume-outline-template.md](references/volume-outline-template.md)
- **章节细纲模板**：[references/chapter-outline-template.md](references/chapter-outline-template.md)
