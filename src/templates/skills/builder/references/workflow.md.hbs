# Builder 完整工作流程

## 概述

Builder 阶段负责从零构建小说的"创作宪法"，包括世界观、角色、文风和情节意向。

---

## 阶段 1：创意收集

### 触发
- 首次初始化项目
- 用户执行 `/builder` 无参数

### 流程

```
1. 启动 brainstorming skill
2. 与用户对话收集：
   - 核心主题（这本书想表达什么？）
   - 类型定位（属于什么类型/子类型？）
   - 核心冲突（主要矛盾是什么？）
   - 目标读者（写给谁看？）
   - 关键设定（有什么独特的设定？）
   - 灵感来源（有什么参考作品？）
3. 整理收集到的信息
4. 生成 core_idea.md
5. 用户确认
```

### 输出
- `core_idea.md` - 核心创意文档

### 用户介入点
- 创意对话：用户主导输入核心创意
- 确认创意：用户确认 core_idea.md 内容

---

## 阶段 2：世界观构建

### 触发
- core_idea.md 确认后自动进入

### 流程

```
1. 读取 core_idea.md
2. 启动 {{cfg "agents.world-builder.count"}} 个 world-builder agents
3. 每个 agent 独立生成世界观方案：
   - Agent 1: 侧重 [方案类型 A，如"宏大史诗"]
   - Agent 2: 侧重 [方案类型 B，如"细腻写实"]
   - Agent 3: 侧重 [方案类型 C，如"反套路"]
4. Agents 进行 {{cfg "agents.world-builder.pk_rounds"}} 轮 PK 辩论
5. 选出最佳方案
6. 展示世界观摘要给用户
7. 用户确认或要求修改
8. 生成最终 world/{{cfg "versioning.initial_version"}}.md
```

### Agent 协调原则

- 每个 agent 有独立上下文，只传递 `core_idea.md` 的关键信息
- 不要传递完整文件内容给 agent（上下文窗口有限）
- 传递时提取要点：主题、类型、核心冲突、关键设定
- PK 辩论时，每个 agent 可以看到其他 agent 的方案摘要（非全文）

### 用户介入点
- 世界观确认：展示世界观摘要，用户确认

---

## 阶段 3：角色架构

### 触发
- 世界观确认后自动进入

### 流程

```
1. 读取 world/{{cfg "versioning.initial_version"}}.md 和 core_idea.md
2. 启动 {{cfg "agents.character-arch.count"}} 个 character-arch agents
3. 每个 agent 基于世界观生成角色方案
4. Agents PK 选优
5. 展示主要角色档案给用户
6. 用户确认或要求修改
7. 生成最终 characters/{{cfg "versioning.initial_version"}}.md
```

### 用户介入点
- 角色档案确认：展示主要角色档案，用户确认

---

## 阶段 4：文风信息采集

### 触发
- 角色确认后自动进入

### 流程

```
1. 检查 author_profile.md 是否存在：
   a. 存在：
      - 读取并展示现有风格
      - 询问："是否沿用你的作家风格？"
      - 选择"是"：提取 author_profile 风格特征，适配本书设定
      - 选择"否"：进入文风采集问卷
   b. 不存在：直接进入文风采集问卷
2. 文风采集问卷（如需要）：
   - 叙事偏好（第一人称/第三人称）
   - 节奏偏好（快节奏/慢节奏）
   - 描写偏好（动作/心理/环境）
   - 对话风格（密集/稀疏）
   - 参考作品风格
3. 生成 style.md
4. 用户确认
```

### 输出
- `style.md` - 本书风格设定

### 用户介入点
- 文风确认：展示本书风格设定，用户确认

---

## 阶段 5：情节意向书

### 触发
- 文风确认后自动进入

### 流程

```
1. 读取所有创作宪法文件：
   - core_idea.md
   - world/{{cfg "versioning.initial_version"}}.md
   - characters/{{cfg "versioning.initial_version"}}.md
   - style.md
2. 生成情节意向书（轻量级，非详细大纲）
3. 展示给用户确认
4. 生成 plot_intent.md
```

### 输出
- `plot_intent.md` - 情节意向书

### 用户介入点
- 情节意向确认：展示情节意向书，用户确认

---

## 阶段 6：冻结版本

### 触发
- 情节意向确认后自动进入

### 流程

```
1. 执行全局一致性检查（见 consistency-check.md）
2. 展示检查结果
3. 用户确认冻结
4. 创建版本标记 v1.0-stable
5. 更新 logs/manifest.md
```

### 用户介入点
- 冻结确认：用户最终确认后冻结版本
