---
name: write
description: "小说写作命令 - 创作有血有肉、打动读者的故事。使用场景：(1) 写作前自动生成细纲 (2) 正文创作（内建去AI味与文学性保证） (3) 批量写作 (4) 查看写作进度 (5) 检测偏离并更新后续细纲。支持命令别名：/w"
disable-model-invocation: true
---

# Write Skill - 创作有灵魂的故事

基于创作宪法（核心创意、世界观、角色）进行小说正文创作，目标是**写出打动读者、有情感深度的文字**，而非机械地完成章节任务。

**核心创作子目标**（每一章都必须追求）：

1. **情感真实性**：角色情感变化符合人物性格与情境逻辑，读者能感受到"这个人活过"
2. **叙事生动性**：细节描写具体可感，场景有画面感，用"烧焦的袜子"代替"战争的恐怖"
3. **语言自然度**：避免 AI 套路化表达，文字有棱角、有呼吸、有人的不完美
4. **读者共鸣**：关键情节能引发读者情感共鸣，而非"正确但空洞"的叙述

> **前置依赖**（按优先级）：
> 1. **完整模式**：`plot_intent.md` + 分卷大纲 → 自动生成细纲后写作
> 2. **极简模式**：`core_idea.md` → 基于核心创意直接写作（适合"pantser"式创作者）
>
> **依赖文件**：
> - `core_idea.md` 或 `plot_intent.md` - 核心创意/情节意向（至少有一个）
> - `world/{{versioning.initial_version}}.md` - 世界观设定（推荐，用于保持一致性）
> - `characters/{{versioning.initial_version}}.md` - 角色档案（推荐，用于保持角色一致性）
> - `outlines/{{versioning.initial_version}}/volume_X_outline.md` - 分卷大纲（可选，有则基于此生成细纲）
> - `chapters/outlines/chapter_XXX.md` - 章节细纲（可选，没有则自动生成）

## 快速开始

```bash
/write                      # 显示帮助菜单（默认）
/write 1                    # 写第 1 章（自动生成细纲）
/w 1                        # 命令别名，同上
/write 1-5                  # 批量写作第 1-5 章（逐章确认）
/write continue             # 继续写下一章
/write --status             # 查看写作进度
/write outline 3            # 只看第 3 章细纲（不写作）
/write check 3              # 检查第 3 章是否偏离细纲
/write undo                 # 撤销上一章的修改
/write history              # 查看章节历史版本
```

---

## 无参数帮助菜单

当用户输入 `/write` 无参数时，显示帮助菜单：

```
【Write Skill - 小说写作命令】

你想做什么？

【写作正文】
  1. /write 1               - 写第 1 章（自动生成细纲）
  2. /write continue        - 继续写下一章
  3. /write 1-5             - 批量写作 1-5 章

【查看细纲】
  4. /write outline 3       - 查看第 3 章细纲
  5. /write outline 1-5     - 查看第 1-5 章细纲

【检查与历史】
  6. /write check 3         - 检查第 3 章是否偏离细纲
  7. /write undo            - 撤销上一章的修改
  8. /write history         - 查看章节历史版本

【进度】
  9. /write --status        - 查看写作进度

【快捷命令】
  /w 是 /write 的快捷别名

💡 提示：
  - 没有细纲时会自动生成，无需担心
  - 写作完成后可以运行 /write check 检查一致性
  - 想继续写下一章？用 /write continue
```

## 核心命令

### `/write N` - 写第 N 章

```bash
/write 1                      # 写第 1 章
/write 5                      # 写第 5 章
```

**执行流程**：
1. 检查是否有第 N 章细纲
2. 没有则基于分卷大纲自动生成细纲
3. 显示细纲摘要供用户确认
4. 开始写作正文

**输出**：
- `chapters/outlines/chapter_NNN.md` - 章节细纲（如之前不存在）
- `chapters/drafts/chapter_NNN.md` - 章节正文

**适用场景**：写作单章

---

### `/write N-M` - 批量写作

```bash
/write 1-5                    # 写第 1 到 5 章
/write 6-10                   # 写第 6 到 10 章
```

**执行流程**：
1. 对于每章，检查是否有细纲
2. 生成缺失的细纲，批量展示给用户确认
3. 用户确认细纲后，**逐章写作正文**（每章完成后等待用户确认再继续）

**输出**：
- `chapters/outlines/chapter_NNN.md` - 各章细纲
- `chapters/drafts/chapter_NNN.md` - 各章正文（逐章生成）

**适用场景**：批量创作多个章节

---

### `/write continue` - 继续写作

```bash
/write continue               # 继续写下一章
```

**执行流程**：
1. 扫描 `chapters/drafts/` 目录，获取所有已写章节
2. 取最大章节号作为 `last_written_chapter`
3. `next_chapter = last_written_chapter + 1`
4. 执行 `/write N` 流程

**适用场景**：按顺序继续写作

---

### `/write --status` - 查看进度

```bash
/write --status
```

**输出**：
- 总章节数 / 已写章节数
- 当前卷进度
- 已写章节列表
- 下一步建议

**适用场景**：查看整体写作进度

---

### `/write outline N` - 查看细纲

```bash
/write outline 3              # 看第 3 章细纲
/write outline 1-5            # 看第 1-5 章细纲
/write outline --regenerate 5 # 重新生成第 5 章细纲
```

**输出**：
- 指定章节的细纲内容

**适用场景**：查看或重新生成细纲

---

### `/write check N` - 检查偏离

```bash
/write check 3                # 检查第 3 章是否偏离细纲
```

**执行流程**：
1. 读取第 N 章细纲和正文
2. 比较差异，判断偏离程度
3. 分析对后续章节的影响
4. 提示用户是否更新后续细纲

**适用场景**：写作完成后检查一致性

---

### `/write undo` - 撤销

```bash
/write undo                   # 撤销第 N 章的修改
/write undo 3                 # 撤销第 3 章的修改
/write history                # 查看章节修改历史
```

**功能**：撤销上一章或指定章节的修改，恢复到前一版本

**适用场景**：对刚写的章节不满意，想重写

---

### `/write history` - 查看历史

```bash
/write history                # 查看所有章节的历史备份
/write history 3              # 查看第 3 章的历史版本
```

**输出**：章节正文的历史版本列表

---

## 感知阶段（写作前首要步骤）

**在生成正文之前，你必须先完成感知：**

### 步骤 1：读取上下文

按以下优先级加载文件：

| 优先级 | 文件 | 说明 |
|--------|------|------|
| P0（必须） | 本章细纲 | `chapters/outlines/chapter_NNN.md` |
| P0（必须） | 上一章末尾 500 字 | `chapters/drafts/chapter_NNN.md`（确保连贯） |
| P0（必须） | `style.md` 风格标签 | 叙事偏好、节奏偏好 |
| P1（推荐） | 出场角色档案摘要 | `characters/v1.0.md`（仅本章出场角色） |
| P1（推荐） | `core_idea.md` | 核心创意一句话 |
| P2（可选） | 世界观相关设定 | `world/v1.0.md`（仅本章涉及部分） |
| P2（可选） | 前文伏笔 | 前几章中未回收的伏笔 |

**Token 预算**：上下文注入总量不超过 7000 token。超出时按 P2 → P1 顺序截断。

### 步骤 2：构建预测模型

输出 100 字以内的**情节预期**（不写入文件，仅用于自检）：

```
【情节预期】
- 核心冲突：[一句话]
- 角色预期行为：[角色名] → [预期行为]
- 伏笔回收：[如有]
- 本章情绪走向：[如"从紧张到释然"]
```

### 步骤 3：惊奇点检测

**生成过程中**，如遇到以下情况，标记为"惊奇"并**暂停**：

| 惊奇类型 | 示例 |
|----------|------|
| 角色行为偏离 | 温和角色突然暴怒；胆小角色主动冲锋 |
| 情节超出细纲 | 细纲未提及的死亡事件；未计划的势力介入 |
| 世界观冲突 | 能力使用超出设定限制；违反已建立的规则 |
| 风格偏离 | 喜剧场景突然变成恐怖；对话风格突变 |

**暂停话术**：
```
⚠️ 惊奇点检测：[具体描述]
- 预期：[预测模型中的内容]
- 实际：[生成中出现的内容]
是否确认这个偏离？（确认 / 修改 / 重新生成）
```

---

## 三明治架构（正文质量保证）

每一章的写作都必须经过以下三层的严格检查。

### 上层面包：输入约束（生成前检查）

**在生成正文前，确认以下要素齐全**（缺少则暂停）：

- [ ] 细纲包含**核心冲突**（一句话概括）
- [ ] 细纲包含**出场角色列表**
- [ ] 细纲包含**关键情节点**（至少 3 个）
- [ ] 细纲包含**本章字数目标**（2500-3500 字范围）
- [ ] 感知阶段的"情节预期"已构建

**缺少时的行为**：
- 缺核心冲突 → 询问用户"本章的核心冲突是什么？"
- 缺角色列表 → 从细纲推断并请用户确认
- 缺情节点 → 询问用户是否需要补充细纲
- 缺字数目标 → 使用默认值 3000 字

---

### 中间的肉：概率生成

**生成规则**：
- 保持创作自由度，不逐句翻译细纲
- 允许对话、描写、节奏的自然发挥
- 但核心事件和角色行为必须受细纲约束
- 写作过程中持续监控**惊奇点**（见感知阶段）

**章节衔接规则**（必须遵守，解决"割裂和突兀"问题）：
- **章首衔接**：开头必须承接上一章的情绪/场景/悬念，禁止无过渡的突兀切入。如果是全书第一章，则需要一个有吸引力的开篇钩子
- **收尾预留**：章末必须设置钩子（悬念/转折/未回收伏笔），为下一章创造叙事入口
- **情绪过渡**：章内情绪转折必须经过中间态过渡（如：紧张 → 犹疑 → 释然），禁止情绪瞬间跳变
- **视角连贯**：保持与上一章相同的叙事视角；如需切换视角，必须用过渡段完成转换

**去AI味守则**（必须遵守，让文字"有骨血"）：

> 详细的 AI-isms 模式列表见 [references/anti-ai-patterns.md](references/anti-ai-patterns.md)

**禁止项**（这些是 AI 写作最常见的"气味"）：

- **禁止 AI 高频词**：不使用宛如、不禁、仿佛、似乎、淡淡的、微微、缓缓地、轻轻的、深沉的、坚定的、温柔的、璀璨的等空洞修饰词。用具体的动作和感官替代
- **禁止三连排比**：不要总是三个形容词、三个并列短语。自然地使用不等长列举，或干脆不用列举
- **禁止万能转折**：避免"不禁X"、"然而Y"、"就在这时Z"的机械过渡。用场景本身推动转折
- **禁止过度平滑**：允许突兀，允许句子长短不一，允许不对称。人类写作有呼吸感和节奏
- **禁止万能乐观**：章节结尾不要总结升华，不要"一切都在向好的方向发展"。留下疑虑、矛盾或未解
- **禁止解释式情感**：不要写"他感到非常愤怒"、"她心中涌起一股悲伤"。用行动和细节让读者自己感受情绪

**鼓励项**（这些是"有骨血"的文字特征）：

- **具体胜于抽象**：写"手指因用力而发白的骨节"而非"紧握的双拳"。写"空气里弥漫着铁锈和焦糊的味道"而非"战场一片狼藉"
- **感官背叛**：描述真实的感官体验而非"理所当然"的想象。雨不总是"冰冷的"——夏天的雨可以是温热的、黏腻的
- **允许冲突和尖锐**：好的文字有棱角。角色可以说蠢话、做错事、表现出狭隘。不要把所有人都写得通情达理
- **句长变异**：短句制造紧张，长句展开氛围。一个 3 字的句子跟在一个 40 字的句子后面，比两个均匀的 20 字句子有力
- **信任读者**：不要把情绪嚼碎了喂给读者。留下空白，让读者自己填充。最好的情感冲击来自读者自己的联想，而非作者的解释
- **对话要有口癖**：每个角色的说话方式应该不同。不要让所有角色都说话得体、逻辑清晰。真实的人说话会跑题、会口误、会欲言又止

---

### 下层面包：输出验证（生成后自动检查）

**生成完成后，立即执行以下验证**（不等待用户触发）：

#### 一致性检查（必须通过）

| 检查项 | 标准 | 失败处理 |
|--------|------|----------|
| 角色行为 | 是否符合 `characters/` 档案 | 自动修正或暂停 |
| 世界观规则 | 是否违反 `world/` 设定 | 自动修正或暂停 |
| 细纲覆盖 | 核心事件是否全部包含 | 补充缺失事件 |
| 前后文连贯 | 与上一章末尾是否衔接 | 修改衔接段落 |

#### 衔接性检查（必须通过）

| 检查项 | 标准 | 失败处理 |
|--------|------|----------|
| 章首衔接 | 是否承接上一章情绪/场景/悬念 | 自动修正开头段落 |
| 章末钩子 | 是否为下一章设置悬念/转折 | 自动补充章末钩子 |
| 情绪过渡 | 转折是否有中间态，无突兀跳变 | 自动插入过渡段 |
| 视角连贯 | 与上一章叙事视角一致或有过渡 | 自动修正视角切换 |

#### 质量检查（建议通过）

| 检查项 | 标准 | 失败处理 |
|--------|------|----------|
| 字数范围 | 在目标字数 ±20% 内 | 提示用户 |
| 节奏控制 | 快慢交替，无连续 500 字纯描写 | 提示用户 |
| 对话自然度 | 对话占比 20%-50%，无大段独白 | 提示用户 |

#### 文学性检查（必须通过 — 去 AI 味）

> 详细模式参考 [references/anti-ai-patterns.md](references/anti-ai-patterns.md)

| 检查项 | 标准 | 失败处理 |
|--------|------|----------|
| AI 高频词扫描 | 无禁用词（宛如、不禁、淡淡的、微微等） | 自动替换为具体描写 |
| 解释式情感 | 无"他感到X"、"她心中涌起Y"的直白表达 | 改为行动/细节暗示情绪 |
| 句长单调 | 无连续 3 句长度差异 < 5 字的情况 | 自动调整句长分布 |
| 对话口癖 | 出场角色对话风格有区分度 | 为对话添加角色特征 |
| 骨血评分 | 至少 3 处具体的感官/动作/物件细节 | 补充具体细节 |

#### 纠偏循环

```
生成初稿 → 自动验证
  ├─ 全部通过 → 展示给用户确认
  ├─ 一致性、衔接性或文学性失败 → 自动修正 → 重新验证（最多 2 次）
  │   ├─ 修正后通过 → 展示给用户确认
  │   └─ 2 次仍失败 → 暂停，向用户报告具体问题
  └─ 质量不达标 → 提示用户，由用户决定是否修正
```

**验证报告格式**：

```
【第 N 章 验证报告】

一致性检查：
✅ 角色行为一致
✅ 世界观规则无冲突
✅ 细纲核心事件已覆盖（3/3）
✅ 前后文衔接自然

衔接性检查：
✅ 章首衔接自然（承接上一章末尾的[具体内容]）
✅ 章末钩子已设置（[具体钩子]）
✅ 情绪过渡平滑
✅ 叙事视角一致

文学性检查（去AI味）：
✅ AI 高频词：未发现禁用词
✅ 情感表达：无解释式情感，用行动/细节暗示
✅ 句长变异：短/中/长句分布合理
✅ 对话口癖：角色说话风格有区分
✅ 骨血评分：★★★★☆（4/5）— 具体细节丰富

质量检查：
⚠️ 字数：4200 字（超出目标 3000 字 ±20%）
✅ 节奏控制合理

总体评价：✅ 通过（1 项建议）

建议：
- 字数偏多，可考虑精简描写段落（非强制）
```

---

## 工作流程

### 写作前检查

```
/write N → 检查 chapter_NNN.md 细纲 → 无则自动生成 → 感知阶段 → 用户确认 → 三明治架构写作
```

**详细流程**：详见 [references/workflows.md](references/workflows.md#写作前检查)

---

### 写作后检查

```
写作完成 → 自动验证 → 通过则保存 / 失败则纠偏重试 → 用户确认
```

**触发时机**：每章写作完成后**自动执行**（不再需要手动触发）

**详细流程**：详见 [references/workflows.md](references/workflows.md#写作后检查)

---

### 进度追踪

```
/write --status → 扫描 chapters/drafts/ → 统计已写章节 → 显示进度和下一步
```

**详细流程**：详见 [references/workflows.md](references/workflows.md#进度追踪)

---

## 输出模板

| 模板 | 用途 | 文件 |
|------|------|------|
| 章节细纲 | 详细事件/节奏/伏笔 | [../plot/references/chapter-outline-template.md](../plot/references/chapter-outline-template.md) |
| 章节正文 | 标准小说格式 | [references/chapter-draft-template.md](references/chapter-draft-template.md) |

---

## 自动处理

### 细纲自动生成

系统自动：
- 写作前检查是否有细纲
- 没有则基于分卷大纲自动生成
- 提示用户确认或修改

### 偏离检测

写作完成后**自动执行**（三明治架构下层面包的一部分）：
- 比较正文与细纲的差异
- 分析对后续情节的影响
- 一致性检查失败时自动修正（最多 2 次）
- 质量不达标时提示用户

### 版本管理

系统自动：
- 修改前自动备份到 `chapters/drafts/archive/`
- 保留最近 3 个备份
- 历史版本归档

---

## 偏离处理

当检测到正文与细纲不一致时：

```
系统：检测到第 N 章正文与细纲有差异：
- 细纲：[细纲内容]
- 正文：[实际内容]

这会影响第 N+1 到 N+3 章的情节设计。

你想：
  1. 自动更新后续细纲（推荐）
  2. 保持原样，我自己改正文
  3. 只看影响分析，稍后决定
```

### 偏离反馈机制

当用户选择“自动更新后续细纲”时，/write 执行：

1. **生成偏离报告**：记录哪些细纲受影响、具体变化内容
2. **更新后续细纲**：基于正文实际内容调整第 N+1 到 N+K 章
3. **标记变更**：在更新后的细纲中注明“因第 N 章偏离，已调整”

当用户选择“保持原样”时：
- 记录偏离但不更新细纲
- 后续章节写作时，**以正文实际内容为准**，而非旧细纲
- 感知阶段加载“上一章末尾 500 字”时自动获取实际内容

---

## 交互式引导

### 首次运行

```
系统：检测到这是你第一次写作！

开始之前：
  1. 生成第 1 章细纲并确认（推荐）
  2. 直接开始写作

推荐先查看细纲，确保情节方向正确。
```

### 细纲确认后

```
系统：✅ 第 N 章细纲已确认！

【本章概述】
[一句话概述]

【核心事件】
1. [事件 1]
2. [事件 2]
3. [事件 3]

直接开始写作？（是/修改细纲/跳过）
```

### 写作完成后

```
系统：✅ 第 N 章已完成！

【验证报告】
✅ 角色行为一致
✅ 世界观规则无冲突
✅ 细纲核心事件已覆盖
⚠️ 字数：XXXX 字（建议范围：XXXX-XXXX 字）

【字数统计】
实际字数：XXXX 字

接下来你可以：
  1. /write continue - 继续写下一章（推荐）
  2. /write outline N+1 - 预览下一章细纲
  3. 修改本章 - 指出需要调整的部分
```

### 批量写作完成后

```
系统：✅ 批量写作完成！

【完成情况】
已写章节：3/5 章

第 1 章：3200 字 ✅
第 2 章：3100 字 ✅
第 3 章：3300 字 ✅
第 4 章：待写
第 5 章：待写

接下来你可以：
  1. /write continue - 继续写第 4 章
  2. /write --status - 查看整体进度
  3. /write check 1-3 - 检查已写章节是否偏离
```

### 偏离检测后

```
系统：检测到第 N 章正文与细纲有差异：

【差异内容】
- 细纲：[细纲中的关键情节]
- 正文：[实际写的内容]

【影响分析】
这会影响第 N+1 到 N+3 章的情节设计：
- 第 N+1 章：[具体影响]
- 第 N+2 章：[具体影响]

你想：
  1. 自动更新后续细纲（推荐）
  2. 保持原样，我自己改正文
  3. 只看影响分析，稍后决定
```

### 进度查看后

```
系统：【写作进度】

总进度：5/30 章 ████████░░░░░░░░░░░░ 17%
当前卷：第一卷

【已写章节】
第 1 章：[标题] - 3200 字
第 2 章：[标题] - 3100 字
第 3 章：[标题] - 3300 字
第 4 章：[标题] - 3000 字
第 5 章：[标题] - 3400 字

【下一步建议】
→ /write 6 - 写第 6 章
→ /write outline 6-8 - 预览下 3 章细纲
```

---

## 依赖检查

当分卷大纲修改时：

```
系统：检测到 volume_1_outline.md 刚刚更新：
- 修改内容：[修改摘要]

这会影响第 X-X 章的细纲。

你想：
  1. 自动重新生成受影响的细纲
  2. 我自己检查，稍后处理
  3. 忽略，继续写作
```

---

## 文件结构

```
chapters/
├── outlines/                # 章节细纲
│   ├── chapter_001.md
│   ├── chapter_002.md
│   └── ...
└── drafts/                  # 章节正文
    ├── chapter_001.md
    ├── chapter_002.md
    └── ...
```

---

## 与 Plot 的配合

| 场景 | 使用工具 |
|------|---------|
| 生成/修改大纲 | /plot |
| 重新生成细纲 | /plot chapters 或 /write outline --regenerate |
| 写作正文 | /write |
| 小幅修改已写章节 | 直接编辑 chapters/drafts/ 下的文件 |
| 修改世界观/角色后更新细纲 | 编辑文件后运行 /write outline --regenerate |

---

## 详细文档

- **工作流程**：[references/workflows.md](references/workflows.md)
- **章节细纲模板**：[../plot/references/chapter-outline-template.md](../plot/references/chapter-outline-template.md)
- **章节正文模板**：[references/chapter-draft-template.md](references/chapter-draft-template.md)
