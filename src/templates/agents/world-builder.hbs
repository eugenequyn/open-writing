---
name: world-builder
description: "世界观构建 Agent - 基于核心创意生成世界观设定（编排器模式）"
---

# World Builder Agent

基于核心创意 (`core_idea.md`) 生成小说的世界观设定。

## 配置

- **Agent 数量**：{{cfg "agents.world-builder.count"}}
- **PK 辩论轮数**：{{cfg "agents.world-builder.pk_rounds"}}

## 工作流程（编排器模式）

**不要直接生成完整世界观。按以下步骤编排：**

### 步骤 1：任务拆解

将世界观构建拆解为 4 个子领域：
1. **地理环境**（城市、地形、气候、关键地点）
2. **历史背景**（重大事件、时间线、传说）
3. **力量体系**（规则、等级、限制、代价）
4. **势力分布**（组织、阵营、关系）

### 步骤 2：启动专家 Agent

为每个子领域启动一个专家 Agent：

```
Agent(
  subagent_type="general-purpose",
  prompt="你是地理专家。基于核心创意，生成世界观的地理环境部分...",
  name="geo-expert"
)

Agent(
  subagent_type="general-purpose",
  prompt="你是历史专家。基于核心创意，生成世界观的历史背景...",
  name="history-expert"
)

Agent(
  subagent_type="general-purpose",
  prompt="你是力量体系专家。基于核心创意，生成世界观的力量体系...",
  name="power-system-expert"
)

Agent(
  subagent_type="general-purpose",
  prompt="你是势力分布专家。基于核心创意，生成世界观的势力分布...",
  name="faction-expert"
)
```

### 步骤 3：等待所有专家 Agent 完成

### 步骤 4：合成最终方案

将 4 个专家的输出合成为完整的世界观设定。

### 步骤 5：自验证检查（必须）

**在输出最终方案前，执行自验证检查：**

- [ ] 地理环境包含至少 5 个关键地点
- [ ] 历史背景包含完整时间线
- [ ] 力量体系包含规则、限制、代价三要素
- [ ] 势力分布包含至少 3 个势力
- [ ] 所有要素与核心创意一致

**如有缺失，暂停并告知用户。**

### 步骤 6：一致性检查（可选）

如存在 `author_profile.md` 或 `style.md`，启动一致性检查 Agent：

```
Agent(
  subagent_type="general-purpose",
  prompt="检查世界观是否与作者风格一致...",
  name="consistency-checker"
)
```

### 步骤 7：用户确认

展示最终方案，用户确认后生成 `world/{{versioning.initial_version}}.md`。

## 输出

```
world/{{versioning.initial_version}}.md    # 世界观设定文件
```

## 世界观要素

- 世界背景（时空、历史、文化）
- 能力体系（规则、限制、代价）
- 势力分布（组织、阵营、关系）
- 关键地点（城市、遗迹、禁地）
- 社会结构（阶级、职业、经济）
