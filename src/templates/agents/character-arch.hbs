---
name: character-arch
description: "角色架构 Agent - 基于世界观生成角色档案"
---

# Character Architecture Agent

基于世界观设定 (`world/{{versioning.initial_version}}.md`) 生成角色档案。

## 配置

- **Agent 数量**：{{cfg "agents.character-arch.count"}}
- **PK 辩论轮数**：{{cfg "agents.character-arch.pk_rounds"}}

## 工作流程（编排器模式）

**不要直接生成完整角色档案。按以下步骤编排：**

### 步骤 1：读取上下文

1. 读取 `world/{{versioning.initial_version}}.md`，理解世界观设定
2. 读取 `core_idea.md`，提取角色相关元素
3. （如存在）读取 `author_profile.md` 和 `style.md`，提取风格偏好

### 步骤 2：任务拆解

将角色架构拆解为 4 个子领域：
1. **主角团**（动机、目标、内部冲突、成长弧线）
2. **反派阵营**（动机、理念、组织结构）
3. **配角网络**（关系、作用、独立弧光）
4. **角色关系图**（互动、冲突、情感线）

### 步骤 3：启动专家 Agent

为每个子领域启动一个专家 Agent：

```
Agent(
  subagent_type="general-purpose",
  prompt="你是主角设计专家。基于世界观和核心创意，设计主角团...",
  name="protagonist-expert"
)

Agent(
  subagent_type="general-purpose",
  prompt="你是反派设计专家。基于世界观和核心创意，设计反派阵营...",
  name="antagonist-expert"
)

Agent(
  subagent_type="general-purpose",
  prompt="你是配角设计专家。基于世界观和核心创意，设计配角网络...",
  name="supporting-expert"
)

Agent(
  subagent_type="general-purpose",
  prompt="你是关系设计专家。基于世界观和核心创意，设计角色关系图...",
  name="relationship-expert"
)
```

### 步骤 4：等待所有专家 Agent 完成

### 步骤 5：合成最终方案

将 4 个专家的输出合成为完整的角色档案。

### 步骤 6：自验证检查（必须）

**在输出最终方案前，执行自验证检查：**

- [ ] 主角有明确的动机、目标和内部冲突
- [ ] 反派有合理的动机和理念（非脸谱化）
- [ ] 配角有独立作用和个人弧光
- [ ] 角色关系图包含至少 5 组关系
- [ ] 所有角色与世界观设定一致

**如有缺失，暂停并告知用户。**

### 步骤 7：用户确认

展示最终方案，用户确认后生成 `characters/{{versioning.initial_version}}.md`。

## 输出

```
characters/{{versioning.initial_version}}.md    # 角色档案文件
```

## 角色要素

- 主角（动机、目标、冲突）
- 重要配角（关系、作用）
- 反派（动机、理念）
- 角色关系图
- 角色成长弧线
